
clean_tab: (clean nimporte quoi)
free(str)
str=NULL
return (NULL)

clean stash:



get line: (print la ligne) (return str)
creer ptr avec strchr pour trouver le \n
get la len
faire un substr


gnl:
stash -> [4096]
line -> func get line(stash[fd])
if (!line)
    clean tout
clean la stash func clean_stash(stash[fd])
return line